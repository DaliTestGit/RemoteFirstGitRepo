# Required metadata
sonar.projectKey=org.sonarqube:Proj1
sonar.projectName=FirstGitRepo:Proj1
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=java

# Encoding of the source files


